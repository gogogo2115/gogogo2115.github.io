"use client";
import { useEffect, useState } from "react";

const Maintenance = () => {
  useEffect(() => {}, []);

  return (
    <div>
      <div id="Maintenance">준비중입니다.</div>
    </div>
  );
};

export default Maintenance;
