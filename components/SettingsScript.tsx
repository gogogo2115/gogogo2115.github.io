/* eslint-disable @next/next/no-sync-scripts */
"use client";

const setInnerScript = ``;

export default function SettingsScript() {
  return <script id="SettingsScript" key="SettingsScript" dangerouslySetInnerHTML={{ __html: setInnerScript }} />;
}
